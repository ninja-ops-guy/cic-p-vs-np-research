# Track BBB: Restriction Argument — ⭐ KEY DISCOVERY

**Verdict:** ❌ Does not close Gap 2 — but reveals the framework is tight.

**Key Theorem (Barrington, 1986): NC¹ = pathwidth-O(1) circuits.**

This means CIC_circuit[O(log n)] = NC¹ exactly. There is NO separation between bounded-pathwidth circuits and NC¹. Proving CIC_circuit(SAT) = ω(log n) IS proving P ⊄ NC¹.

**Implications:**
- The conditional path has zero slack
- No restriction argument can bypass the P ⊄ NC¹ barrier
- The framework is internally consistent but doesn't provide a "softer" target

**Full analysis:** See `/mnt/agents/output/track_bbb_restriction_depth.md`
